%tr
  = raw table_row(delivery.id)
  = raw table_row(Delivery::PRIORITIES[delivery.priority])
  = raw table_row(datetime_formatted(delivery.scheduled_delivery_time))
  = raw table_row(datetime_formatted(delivery.delivered_on))
  = raw table_row(Delivery::STATES[delivery.state])
  = raw table_row(delivery.delivered_items.andand.size)
  %td
    = link_to_icon(:edit, edit_polymorphic_path([@recipient, delivery]))
    |
    = link_to_icon(:delete, polymorphic_path([@recipient, delivery]))
    |
    = link_to(image_tag("deliver.png", :title => "Delivery Sheet"), polymorphic_path([:delivery_sheet, @recipient, delivery]), { :popup => ['new_window', 'height=700,width=1050,menubar=yes,scrollbars=1'] })
